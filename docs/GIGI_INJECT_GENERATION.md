# Nota (Layer Zero)

Il flusso “packer + script shell” per iniettare profilo/IPA è stato rimosso in favore della struttura essenziale documentata in **`LAYER_ZERO.md`**.

La radice CA per il controllo locale resta in **`gigi_ca/`** (`gigi_root.crt`, `gigi_root.key`). Il profilo XML di riferimento è **`gigi_killer.mobileconfig`** nella root del repo.
